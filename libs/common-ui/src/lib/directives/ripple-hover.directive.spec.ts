import { RippleHoverDirective } from './ripple-hover.directive';

describe('RippleHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new RippleHoverDirective();
    expect(directive).toBeTruthy();
  });
});
