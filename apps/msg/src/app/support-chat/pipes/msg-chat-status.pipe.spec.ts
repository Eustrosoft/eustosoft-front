import { MsgChatStatusPipe } from './msg-chat-status.pipe';

describe('MsgChatStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new MsgChatStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
